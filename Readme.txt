For the backend :- 

	CORS module is installed and entity framework is also installed. 
	Migration need to be added by using command :- add-migration and for database the command :- update database needs to added 


For the UI:- 
	Its working on localhost 4200 all the packages are already installed 
	use the command "ng serve" on CMD to run the UI.
